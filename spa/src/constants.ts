import { QueryClient } from "@tanstack/react-query";

export const queryClient = new QueryClient();
export const BASE_URL = "https://asqit-calendar.deno.dev";
// export const BASE_URL = "http://localhost:8000";
